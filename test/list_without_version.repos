# The list.repos import will create a repo under immutable/tag
# which will be checked out to a specific tag. This repo file
# leaves the version unspecified so that vcs import will leave
# it alone when --skip-existing is provided.

repositories:
  without_version:
    type: git
    url: https://github.com/dirk-thomas/vcstool.git
